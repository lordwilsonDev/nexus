__pycache__/
*.pyc
.pytest_cache/
.DS_Store

# Per-run hygiene experiment artifacts (ephemeral); keep aggregate + gate
artifacts/hygiene/n0*_*.json

# Runtime vector-DB state — never commit
/data/

.coverage

# mutmut build artifacts — evidence is the mutation_score.json snapshot, not the mutant tree
mutants/
